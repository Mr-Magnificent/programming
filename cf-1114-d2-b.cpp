#include <bits.stdc++.h>
using namespace std;
#define ll long long int

int main() {
	ll n, m, k;
	cin >> n >> m >> k;
	ll arr[n];
	for (ll i = 0; i < n; i++) {
		cin >>arr[i];
	}
	for (ll i = 0; i < n; i++) {
		
	}
}